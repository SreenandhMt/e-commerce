import 'package:flutter/material.dart';

class AppColors {
  // Brand Colors (Monochrome Luxury Theme)
  static const primaryLight = Color(0xFF111111);
  static const primaryDark = Colors.white;

  static const lightBackground = Colors.white;
  static const darkBackground = Color(0xFF0A0A0A); // Classic obsidian black background

  static const lightSurface = Colors.white;
  static const darkSurface = Color(0xFF121212); // Luxury charcoal surface

  static const textPrimaryLight = Color(0xFF111111);
  static const textPrimaryDark = Colors.white; // High-contrast pure white

  static const textSecondaryLight = Color(0xFF6E6E73);
  static const textSecondaryDark = Color(0xFF8E8E93); // Classic muted mid-grey

  static const borderLight = Color(0xFFE5E5E5);
  static const borderDark = Color(0xFF222222); // Fine charcoal border
  
  static const imageBgLight = Color(0xFFF7F7F7);
  static const imageBgDark = Color(0xFF121212); // Image container backing
}
